{
  "name": "DiKeVoi-Mono",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently --kill-others --names \"BACKEND,FRONTEND\" --prefix-colors \"bgGreen.bold,bgBlue.bold\" \"cd backend && uvicorn app.main:app --reload\" \"cd frontend && npm start -- -c\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
